# Hoop Central API (required)
HOOP_CENTRAL_API_URL=https://hoopcentral-50-production.up.railway.app
# Local dev alternative:
# HOOP_CENTRAL_API_URL=http://localhost:3001

# Optional — only needed if INGEST_API_KEY is set on the server
INGEST_API_KEY=

# BRef rate limiting — wider spacing avoids blocks better than long cooldowns
SCRAPE_REQUEST_DELAY_MS=15000
SCRAPE_INDEX_DELAY_MS=20000

# 0 = no forced wait on resume; rely on spacing above
BREF_COOLDOWN_HOURS=0
